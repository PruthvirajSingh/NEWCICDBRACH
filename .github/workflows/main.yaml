name: CINAME
on: [push] 
jobs:
  run-git-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Simple Js Actions
        id: greet
        uses: PruthvirajSingh/NEWCICDBRACH@master
        with:
          who-to-greet: pruthviraj
      - name: Log Gretting Time
        run: echo "${{ steps.greet.outputs.time }}"